export enum WorkType {
  'REMOTE' = 'remote',
  'OFFICE' = 'office',
  'MOVE' = 'ready to move',
  'HYBRID' = 'hybrid',
  'DEFAULT' = 'default',
}

export const WorkTypeMap = [
  WorkType.REMOTE,
  WorkType.OFFICE,
  WorkType.MOVE,
  WorkType.HYBRID,
  WorkType.DEFAULT,
];
